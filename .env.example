# Backend API (include /api). In dev you can use the Vite proxy:
VITE_API_URL=/api

# Or direct to backend:
# VITE_API_URL=http://localhost:3000/api

# Show extra hints when developing outside monday.com iframe
VITE_MONDAY_DEV_MODE=true
